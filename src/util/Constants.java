package util;

public class Constants {
    public static int MSUP = 2;     // ngưỡng hỗ trợ tối thiểu
    public static double T = 0.2;   // ngưỡng xác suất tối thiểu
    public static double ALPHA = 0.6; // hệ số pruning xấp xỉ
    public static double MIN_AVG_WEIGHT = 0.0;
}
